## Console Menu by *m1ad3n*
#
#### Function Overview
```c
int menu(char _indicator, int _count, ...);
```

#### Arguments
```c
char _indicator; // the character that goes before selcted item in menu
int _count; // how many menu items are there
// ... - Menu items as char* (string)
```